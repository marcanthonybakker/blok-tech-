{
  "name": "seniorenmatch",
  "version": "1.0.0",
  "description": "applicatie voor senioren met hobby's om te matchen",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "precompress": "{{ a }}",
    "compress": "{{ a }}",
    "postcompress": "{{ a }}"
  },
  "author": "Marc Anthony Bakker",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}

